one post handlebars

<div class="post">
  <div class="card">
    <div class="card-header flex-row">
      <h2>{{post.post_title}}</h2>
    </div>
    <div class="card-body">
      {{post.post_text}}
    </div>
  </div>